{
    "name": "pppcreative/select-badge",
    "description": "Package combines Select field and Badge field, adds styled classes in CSS and adds functions such as icons(), map(), addTypes(), options(), labels(). Added to BelongsBaadge !",
    "keywords": [
        "laravel",
        "select",
        "badge",
        "selectBadge",
        "status"
    ],
    "type": "library",
    "license": "MIT",
    "minimum-stability": "dev",
    "require": {
    },
    "autoload": {
        "psr-4": {
            "Wame\\SelectBadge\\": "src/"
        }
    },
    "extra": {
        "laravel": {
            "providers": [
                "Wame\\SelectBadge\\SelectBadgeServiceProvider"
            ]
        }
    }
}
